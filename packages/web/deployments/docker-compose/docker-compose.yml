version: '3.7'
services:
  crm-dashboard:
    container_name: qilin-store-web
    image: p1hub/qilin-store-web:${TAG}
    restart: on-failure
    networks:
      - default
networks:
  default:
    name: ${DOCKER_NETWORK}
    external: true
